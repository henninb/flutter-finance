<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all certificates for development -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-installed certificates (for development) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <!-- Allow all traffic to local/development domains -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">bhenning.com</domain>
        <domain includeSubdomains="true">finance.bhenning.com</domain>
        <domain includeSubdomains="true">192.168.10.10</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
